[project]
name = "generative-ai-trust-guard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "huggingface-hub>=1.5.0",
    "python-dotenv>=1.2.1",
    "trustguard>=0.2.1",
]
